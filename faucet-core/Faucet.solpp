pragma soliditypp ^0.4.3;

contract Faucet {

  event MyLog(address indexed addr, string log);

  onMessage sayHello(address dest) payable {
    dest.transfer(msg.tokenid, msg.amount);
    emit MyLog(dest, "Have some vite");
  }
}
